services:
  - type: web
    name: voisins-connect-backend
    env: rust
    plan: free
    buildCommand: cargo build --release
    startCommand: ./target/release/hello_world_backend
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: MONGODB_URL
        sync: false
      - key: JWT_SECRET
        sync: false
        generateValue: true
      - key: PORT
        value: "3001"
    # Specify the root directory
    directory: backend
